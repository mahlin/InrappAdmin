@using InrappAdmin.Web.Models.ViewModels
@model InrappAdmin.Web.Models.ViewModels.SystemViewModels.SystemViewModel
@{
    ViewBag.Title = "FAQ";
}

<script type="text/javascript">

    $(document).ready(function () {
        $('#FragorOchSvar').hide();
    });

    $(function () {
        $("#txtKommunkod").focus();
    });

    function showFAQs(catId) {
        //alert(catId);
        $('#SelectedFAQCategory').val(catId);
        //$('#SelectedFAQCategory').val(1);
        alert($('#SelectedFAQCategory').val());
        $('#FragorOchSvar').show();
    }

    $(function () {
        $('.edit-mode').hide();
        $('.edit-user, .cancel-user').on('click',
            function () {
                var tr = $(this).parents('tr:first');
                tr.find('.edit-mode, .display-mode').toggle();
            });
        $('.save-user').on('click',
            function () {
                var tr = $(this).parents('tr:first');
                var categoryId = tr.find("#FAQCatId").html();
                var category = tr.find("#FAQCategory").val();
                var categorySkapadDatum = tr.find("#CategorySkapadDatum").val();

                //Creating AdmFAQKategori JSON object
                var admCategory =
                    {
                        "Id": categoryId,
                        "Namn": category,
                        "SkapadDatum": categorySkapadDatum
                    };
                //Posting ApplicationUser object to controller's Update action method
                $.ajax({
                    url: '/System/UpdateFAQCategory',
                    data: JSON.stringify(applicationUser),
                    type: 'POST',
                    contentType: 'application/json; charset=utf-8',
                    success: function (data) {
                        //location.reload();
                        $("#pageContainer").html(data);
                        tr.find('.edit-mode, .display-mode').toggle();
                        $('#footer').hide();
                        //alert('Record updated Successfully!!');
                    }
                });

            });

        $('.edit-user').on('click',
            function () {
                var tr = $(this).parents('tr:first');
                var categoryId = tr.find("#FAQCatId").html();
                var category = tr.find("#FAQCategory").val();
                var categorySkapadDatum = tr.find("#CategorySkapadDatum").val();

                tr.find("#lblFAQCategory").text(category);
                tr.find("#lblSkapadDatum").text(categorySkapadDatum);
            });
    });
</script>

<style type="text/css">
    .edit-mode {
        font-weight: normal;
    }

    .edit-user {
        font-weight: normal;
    }

        .edit-user display-mode {
            font-weight: normal;
        }

    .save-user edit-mode {
        font-weight: normal;
    }

    .display-mode {
        font-weight: normal;
    }

    .cancel-user {
        font-weight: normal;
    }

    .webgrid-table {
        font-family: Arial,Helvetica,sans-serif;
        font-size: 14px;
        font-weight: normal;
        width: 950px;
        display: table;
        border-collapse: collapse;
        border: solid px #C5C5C5;
        background-color: white;
    }

        .webgrid-table td, th {
            border: 1px solid #C5C5C5;
            padding: 3px 7px 2px;
        }

    .webgrid-header, .webgrid-header a {
        background-color: #f9f9f9;
        color: #494A4A;
        text-align: left;
        text-decoration: none;
    }

    .webgrid-footer {
    }

    .webgrid-row-style {
        padding: 3px 7px 2px;
    }

    .webgrid-alternating-row {
        background-color: #f9f9f9;
        padding: 3px 7px 2px;
    }

    .col1Width {
        width: 200px;
        margin: -5px;
        padding: 0;
    }

    .col2Width {
        width: 200px;
        margin: -5px;
        padding: 3px 7px 2px;
    }

    .col3Width {
        width: 220px;
        margin: -5px;
        padding: 0;
    }

    label {
        font-weight: normal;
        width: 200px;
    }
</style>

<div id="pageContainer">
    <div class="row">
        <div class="col-md-8 padding-top-55">
            <h4>Editera FAQ</h4>
            <br />

            @if (Model != null)
            {
                @Html.HiddenFor(m => m.SelectedFAQCategory)
                @Html.ActionLink("Skapa ny FAQ-kategori", "CreateFAQCategory", "System")


                <div id="gridContent" style="font-family: Arial; padding: 20px;">
                    @{
                        ViewBag.Title = "Administrera FAQ-kategorier";
                        var grid = new WebGrid(source: Model.FAQCategories, canPage: true, rowsPerPage: 10, ajaxUpdateContainerId: "faqCatsGrid");
                        grid.Pager(WebGridPagerModes.All);
                    }


                    @grid.GetHtml(tableStyle: "webgrid-table",
                        headerStyle: "webgrid-header",
                        footerStyle: "webgrid-footer",
                        alternatingRowStyle: "webgrid-alternating-row",
                        selectedRowStyle: "webgrid-selected-row",
                        rowStyle: "webgrid-row-style",
                        mode: WebGridPagerModes.All,
                        columns: grid.Columns(
                            grid.Column("ID", format: @<text><span class="display-mode">@item.Id</span>
                                                          <label id="FAQCatId" class="edit-mode col2Width">@item.Id</label></text>, style: "col1Width"),
                            grid.Column("Kategori", "Kategori", format: @<text><span class="display-mode"><label id="lblFAQCategory">@item.Kategori</label></span>
                                                                            <input type="text" id="FAQCategory" value="@item.Kategori" class="edit-mode form-control"/></text>, style: "col2Width"),

                            grid.Column("Skapad", "SkapadDatum", format: @<text> <span class="display-mode">
                                                                                 <label id="lblSkapadDatum">@item.SkapadDatum</label></span>
                                                                             <input type="text" id="CategorySkapadDatum" value="@item.SkapadDatum" class="edit-mode form-control"/></text>, style: "col2Width"),

                            grid.Column("Action", format: @<text>
                                                              <button class="edit-user display-mode btn btn-default">Ändra</button>
                                                              <button class="save-user edit-mode btn btn-default">Spara</button>
                                                              <button class="cancel-user edit-mode btn btn-default">Avbryt</button>
                                                              <button class="btn btn-primary" onclick="showFAQs(@item.Id); return false;">@HttpUtility.HtmlDecode("Administrera faq-frågor &raquo;")</button>  
                                                              @*@Html.ActionLink(HttpUtility.HtmlDecode("Administrera faq-frågor &raquo;"), "GetOrganisationsContacts", "System", null, new { @class = "btn btn-primary" })*@

                                                           </text>, style: "col3Width", canSort: false)))
                </div>
            }
        </div>
    </div>
</div>

<div id="FragorOchSvar">
    @Html.Partial("_EditFAQs") 
</div>





